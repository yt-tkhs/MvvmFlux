<?xml version="1.0" encoding="utf-8"?>
<navigation android:id="@+id/graph_task_detail"
            xmlns:android="http://schemas.android.com/apk/res/android"
            xmlns:app="http://schemas.android.com/apk/res-auto"
            app:startDestination="@id/taskDetail"
    >

    <fragment
        android:id="@+id/taskDetail"
        android:name="app.ytak.tasks.feature.taskdetail.TaskDetailFragment"
        >

        <argument
            android:name="taskId"
            app:argType="string"
            />
    </fragment>
</navigation>